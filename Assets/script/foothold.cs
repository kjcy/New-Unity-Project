using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class foothold : Enemy
{
    // Start is called before the first frame update
    void Start()
    {
        hp = 999;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
